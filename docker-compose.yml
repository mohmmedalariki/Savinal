version: '3.8'

services:
  bot:
    build: .
    container_name: savinal_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PYTHONUNBUFFERED=1
